"# Implementation Plan: CMS for Production Standards + Device Image Upload + Footer Fix\n\n## Summary\nThree focused improvements:\n1. **CMS tab for \"Our Production Standards\"** — Add a full \"Tools\" tab in the Admin Dashboard to add/edit/delete tool cards (name + icon), with device image upload support\n2. **Device image upload in all CMS image fields** — Every image field in the admin gets a \"Upload from device\" button alongside the URL input\n3. **Footer fix** — The footer currently shows just copyright + admin link floating in empty space. Needs a proper structured footer with logo, contact info, social links and copyright bar\n\n---\n\n## Proposed Changes\n\n### 1. AdminDashboard.tsx — Add \"Tools\" tab + device upload on image fields\n\n#### [MODIFY] AdminDashboard.tsx\n\n- Add `\"tools\"` to the tab union type\n- Add a **Tools tab** in the sidebar nav\n- Add Tools tab content: list of current tools with edit/delete, plus an \"Add Tool\" form with:\n  - Name text input\n  - Icon field: URL input **AND** a \"📁 Upload from device\" button that triggers file picker → calls `uploadMedia()` → sets the URL\n- Same image-upload pattern applied to all existing image fields (films, testimonials, team, leadership)\n\n### 2. CMSContext.tsx — Add `updateToolItem` helper for tools array (non-id based)\n\nTools use a simple array of `{ name, icon }` without IDs. We need a way to update/delete them by index.\n\n#### [MODIFY] CMSContext.tsx\n- Add `updateToolByIndex(index: number, updated: ToolItem) => void`\n- Add `deleteToolByIndex(index: number) => void`  \n- Add `addTool(item: ToolItem) => void`\n- Expose these in context\n\n### 3. Contact.tsx — Fix the footer\n\n#### [MODIFY] Contact.tsx\n- Replace the bare `footer-copyright` div with a proper **3-column footer layout**:\n  - **Col 1**: Pooja Productions logo/name + tagline\n  - **Col 2**: Quick links (Home, About, Film, Studio, Contact)\n  - **Col 3**: Contact info (email, phone)\n- **Bottom bar**: copyright text + Admin Login link\n- Styled w
<truncated 346 bytes>